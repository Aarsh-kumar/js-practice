"use strict"; //treat all js code as newer version(aajkl by default hi 
// mostly newer version hi use krte)

// alert (3+3) // we are using nodejs not browser

console.log(3
    +3) // code readability should be high

    console.log("harwinder")

    let name= "harwinder"

    let age= 18

    let isLoggedIn =false

    console.table({name, age, isLoggedIn})

    // number => 2 to the power 53
    // bigint = for big numbers
    // string => ""
    // boolean =trueorfalse
    // null = standAloneValue
    // undefined => not assigned
    // symbol=> unique (used in apps like figma)
    // object= will learn further

    /* console.log(typeof) // to find the types of datatype in the tags
    */

    console.log(typeof age )
    console.log(typeof null)
    console.log(typeof name)
    console.log(typeof isLoggedIn )
    

